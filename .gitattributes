###############################################################################
# Core line ending & text handling
###############################################################################

# Normalize all text files to LF in the repo, convert to native on checkout
* text=auto eol=lf

# Explicitly mark common text file types
*.java      text
*.kt        text
*.groovy    text
*.xml       text
*.gradle    text
*.properties text
*.md        text
*.txt       text
*.csv       text
*.json      text
*.yml       text
*.yaml      text
*.html      text
*.css       text
*.js        text
*.ts        text
*.sh        text eol=lf
*.bat       text

###############################################################################
# Cucumber & Testing
###############################################################################

# Treat Cucumber feature files as text and mark their language for GitHub
*.feature   text linguist-language=Cucumber

# Treat Gherkin-like specification files as text
*.story     text

###############################################################################
# Binary files (no diffs, no merges)
###############################################################################

# Images
*.png       binary
*.jpg       binary
*.jpeg      binary
*.gif       binary
*.bmp       binary
*.ico       binary
*.svg       text

# Fonts
*.ttf       binary
*.otf       binary
*.woff      binary
*.woff2     binary

# Archives and bundles
*.zip       binary
*.tar       binary
*.gz        binary
*.7z        binary
*.jar       binary
*.war       binary
*.ear       binary

# Executables
*.exe       binary
*.dll       binary
*.so        binary
*.dylib     binary

###############################################################################
# Git Large File Storage (LFS) — OPTIONAL
# Uncomment if you decide to use Git LFS for large assets
###############################################################################
# *.zip      filter=lfs diff=lfs merge=lfs -text
# *.jar      filter=lfs diff=lfs merge=lfs -text
# *.png      filter=lfs diff=lfs merge=lfs -text
# *.jpg      filter=lfs diff=lfs merge=lfs -text

###############################################################################
# Documentation & Markup
###############################################################################

# Improve language detection for GitHub linguist (optional tuning)
*.md        linguist-language=Markdown
*.adoc      text linguist-language=AsciiDoc

###############################################################################
# IDE / Tooling specific behaviour (usually combined with .gitignore)
###############################################################################

# IntelliJ IDEA project files are text (if ever committed)
*.iml       text
*.ipr       text
*.iws       text

# VS Code settings
*.code-workspace text

###############################################################################
# Scripts & CI
###############################################################################

# Shell scripts should always use LF and be treated as text
*.sh        text eol=lf

# PowerShell scripts
*.ps1       text

# GitHub Actions workflows (YAML)
.github/**/*.yml   text
.github/**/*.yaml  text

###############################################################################
# Misc / Safety
###############################################################################

# Make sure these stay as text for easier review
*.log       text
*.conf      text
*.ini       text